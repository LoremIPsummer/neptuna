import React from 'react';
import styles from './RegisterSidebar.module.scss';

const RegisterSidebar: React.FC = () => (
  <div className={styles.RegisterSidebar}>
    RegisterSidebar Component
  </div>
);

export default RegisterSidebar;
